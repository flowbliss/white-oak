generate_projects:
  blocks:
    - block_name: prod
      terragrunt: true
      root_dir: "environments/prod/us-east-1/"
      workflow: default

workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - init
        - apply

